# F1 Race Replay - Interactive Visualization Tool
# Python Dependencies
# ====================================
# Compatible with Python 3.10+

# Core F1 Data Library
# FastF1 provides access to F1 timing data, telemetry, and session information
# Uses Ergast API and official F1 timing data
fastf1>=3.0.0

# Web Framework
# FastAPI is a modern, fast (high-performance) web framework for building APIs
# Based on standard Python type hints
fastapi>=0.100.0

# ASGI Server
# Uvicorn is a lightning-fast ASGI server implementation
# Required to run FastAPI applications
uvicorn[standard]>=0.20.0

# Data Processing
# Pandas for data manipulation and analysis
# NumPy for numerical computations
pandas>=2.0.0
numpy>=1.24.0

# HTTP Client
# For making external API requests if needed
httpx>=0.24.0

# CORS Middleware Support
# Already included in FastAPI, but explicit for clarity

# Caching
# For caching F1 data to avoid repeated API calls
diskcache>=5.6.0

# Type Validation
# Pydantic for data validation (comes with FastAPI)
pydantic>=2.0.0

# Date/Time Handling
python-dateutil>=2.8.0
